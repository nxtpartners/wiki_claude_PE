---
// Standalone full-width deliverable: the AI Value Creation Matrix.
// A tool built with Claude Code. Its guide page explains what it shows about
// what private equity professionals can build.
import DeliverableLayout from '../../layouts/DeliverableLayout.astro';
import ValueCreationMatrix from '../../components/examples/ValueCreationMatrix.astro';
import { withBase } from '../../utils/withBase';

const walkthroughHref = '/claude-code/example-value-creation';
---

<DeliverableLayout
  title="AI Value Creation Matrix"
  description="A filterable matrix of AI value-creation use cases across five private equity value levers, with hold-phase, fund-tier, and KPMG service views."
  walkthroughHref={walkthroughHref}
  walkthroughLabel="the guide page"
  subtitle={`A structured, filterable inventory of AI value-creation use cases across five private equity value levers. Filter by hold phase, fund tier, and KPMG service line to see where each use case fits. It was built with Claude Code; the <a href="${withBase(walkthroughHref)}">guide page</a> explains what it shows about what you can build.`}
>
  <ValueCreationMatrix />
</DeliverableLayout>
